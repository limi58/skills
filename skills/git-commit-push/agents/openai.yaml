interface:
  display_name: "Git Commit & Push"
  short_description: "Safely commit all current workspace changes and push to the current origin branch after explicit confirmation"
  default_prompt: "Use $git-commit-push to commit all current workspace changes and push to the current origin branch."

policy:
  allow_implicit_invocation: false
