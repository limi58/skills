interface:
  display_name: "Git 提交并推送"
  short_description: "安全提交当前工作区全部改动，并在明确确认后推送到 origin 当前分支"
  default_prompt: "使用 $git-commit-push 提交当前工作区的全部改动，并推送到 origin 的当前分支。"

policy:
  allow_implicit_invocation: false
