interface:
  display_name: "Windows UTF-8 Shell"
  short_description: "Use PowerShell 7 and UTF-8 consistently for commands on Windows"
  default_prompt: "Use $windows-utf8-shell to safely run commands on Windows with PowerShell 7 and UTF-8."

policy:
  allow_implicit_invocation: true
